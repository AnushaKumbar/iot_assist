# IoT Assist – Environment Configuration
# Copy this file to .env and fill in real values when connecting the backend.

# Backend API base URL (Node.js / Express)
# Leave empty to use mock services (frontend-only mode)
VITE_API_BASE_URL=

# Example when backend is running:
# VITE_API_BASE_URL=http://localhost:3001
